test("context works", () => {
  // teste den NotificationContext!
  //
  //   - überprüfe z.B. das die initiale messageId 'null' ist
  //   - zeige dann die 'not_found' notification an
  //   - stelle dann sicher, dass die messageId und/oder message korrekt gesetzt ist
});
